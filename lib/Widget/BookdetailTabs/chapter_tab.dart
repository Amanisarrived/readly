import 'package:flutter/material.dart';

class ChapterTab extends StatelessWidget {
  const ChapterTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
